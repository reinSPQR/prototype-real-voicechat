// sad — droopy filled eyes (outer corners pulled down), strong frown.
//
// Reference (ezgif-split/sad.png): filled cyan ovals tilted so the outer
// corners drop, plus a clear frown ⌢. There is no separate brow mesh —
// the dark area above each eye is just the screen showing through where
// the squashed/tilted eye no longer reaches.

export const SadExpression = {
  eyeFillAlpha:  1,
  specularAlpha: 1,
  eyeOpen:  0.75,
  eyeTiltL: -0.14,
  eyeTiltR: +0.14,

  mouthCurl:  -0.7,
  mouthWidth:  0.85,
};
